<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>Me.EtienneDx</groupId>
	<artifactId>real-estate</artifactId>
	<version>1.4.5</version>

	<name>RealEstate</name>
	<description>A spigot plugin for selling, renting, leasing and auctioning GriefPrevention and GriefDefender claims</description>

	<properties>
		<!-- Target Minecraft/Paper/Spigot version. Override any of these
		     together (they must describe the same Minecraft version) to
		     build against a different target, e.g.:
		       mvn package -Dminecraft.api.version=1.21.11 \
		                   -Dspigot.api.version=1.21.11-R0.1-SNAPSHOT \
		                   -Dpaper.api.version=1.21.11-R0.1-SNAPSHOT \
		                   -Dskip.spigot.api=false
		     Minecraft/Paper moved to a new calendar-based version scheme
		     after 1.21.11 (e.g. 26.1.2). Spigot itself has not published a
		     spigot-api under that scheme; as of this writing 1.21.11 is
		     the newest Minecraft version with a spigot-api release, so the
		     26.x target below is Paper-only (skip.spigot.api=true, the
		     default). -->
		<minecraft.api.version>26.1.2</minecraft.api.version>
		<paper.api.version>[26.1.2.build,)</paper.api.version>
		<spigot.api.version>26.1.2-R0.1-SNAPSHOT</spigot.api.version>
		<!-- No spigot-api is published for the 26.x scheme, so it's excluded
		     from the default (latest/primary) build target. Set to false
		     for older, pre-26.x targets where spigot-api and paper-api
		     coexist. -->
		<skip.spigot.api>true</skip.spigot.api>
	</properties>

	<build>
        <finalName>${project.name}</finalName>
		<sourceDirectory>src</sourceDirectory>
		<resources>
			<resource>
				<directory>.</directory>
				<filtering>true</filtering>
				<targetPath>.</targetPath>
				<includes>
					<include>plugin.yml</include>
					<include>paper-plugin.yml</include>
				</includes>
			</resource>
			<resource>
				<directory>./resources</directory>
				<targetPath>./resources</targetPath>
			</resource>
		</resources>
		<plugins>
			<plugin>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.13.0</version>
				<configuration>
					<!-- Paper's own build (as of the 26.x version scheme) compiles
					     against a JDK 25 toolchain (see PaperMC/Paper's
					     build.gradle.kts and .github/workflows/build.yml), and
					     WorldGuard/WorldEdit/Towny at their latest versions publish
					     JDK 25 (class file 69) bytecode, unreadable by the
					     previous JDK 16 toolchain. JDK 25 reads all of that plus
					     everything older, so it's a single toolchain that covers
					     every dependency across every target version. -->
					<release>25</release>
				</configuration>
			</plugin>
			<plugin>
			  <groupId>org.apache.maven.plugins</groupId>
			  <artifactId>maven-javadoc-plugin</artifactId>
			  <version>3.3.2</version>
			  <configuration>
			    <additionalClasspathElements>
			      <additionalClasspathElement>${settings.localRepository}/net/kyori/adventure-api/4.18.0/adventure-api-4.18.0.jar</additionalClasspathElement>
			      <additionalClasspathElement>${settings.localRepository}/net/kyori/adventure-text-serializer-plain/4.18.0/adventure-text-serializer-plain-4.18.0.jar</additionalClasspathElement>
			    </additionalClasspathElements>
			    <!-- Optionally disable doclint if needed -->
			    <additionalOptions>
			      <additionalOption>-Xdoclint:none</additionalOption>
			    </additionalOptions>
			  </configuration>
			</plugin>
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<version>2.4.1</version>
				<configuration>
					<finalName>${project.name}-${project.version}</finalName>
    				<appendAssemblyId>false</appendAssemblyId>
					<descriptorRefs>
						<descriptorRef>jar-with-dependencies</descriptorRef>
					</descriptorRefs>
					<archive>
						<manifest>
							<mainClass>sampler.Main</mainClass>
						</manifest>
					</archive>
				</configuration>
				<executions>
					<execution>
						<id>make-assembly</id>
						<phase>package</phase> 
						<goals>
							<goal>single</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
	<repositories>
		<repository>
			<id>spigot-repo</id>
			<url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
		</repository>
		<repository>
			<id>bungeecord-repo</id>
			<url>https://oss.sonatype.org/content/repositories/snapshots</url>
		</repository>
		<repository>
			<id>jitpack.io</id>
			<url>https://jitpack.io</url>
		</repository>
		<repository>
			<id>vault-repo</id>
			<url>https://nexus.hc.to/content/repositories/pub_releases</url>
		</repository>
		<repository>
	        <id>aikar</id>
	        <url>https://repo.aikar.co/content/groups/aikar/</url>
    	</repository>
    	<repository>
	        <id>ess-repo</id>
	        <url>https://ci.ender.zone/plugin/repository/everything/</url>
	    </repository>
		<repository>
	        <id>glaremasters</id>
	        <url>https://repo.glaremasters.me/repository/bloodshot</url>
	    </repository>
	    <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <repository>
        	<id>sk89q-repo</id>
        	<url>https://maven.enginehub.org/repo/</url>
    	</repository>
	    <repository>
	      <id>glaremasters repo</id>
	      <url>https://repo.glaremasters.me/repository/towny/</url>
	    </repository>
	    <repository>
          <id>adventure</id>
          <url>https://repo1.maven.org/maven2/</url>
	    </repository>
	    <repository>
	        <id>placeholderapi</id>
	        <url>https://repo.extendedclip.com/content/repositories/placeholderapi/</url>
	    </repository>
	</repositories>
	<pluginRepositories>
		<pluginRepository>
			<id>maven-snapshots</id>
			<url>https://repository.apache.org/content/repositories/snapshots/</url>
		</pluginRepository>
	</pluginRepositories>
	<dependencies>
		<dependency>
			<groupId>net.milkbowl.vault</groupId>
			<artifactId>VaultAPI</artifactId>
			<version>1.7</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
	        <groupId>co.aikar</groupId>
	        <artifactId>acf-bukkit</artifactId>
	        <version>0.5.1-SNAPSHOT</version>
	    </dependency>
	    <dependency>
	        <groupId>net.ess3</groupId>
	        <artifactId>EssentialsX</artifactId>
	        <version>2.16.1</version>
	        <scope>provided</scope>
	        <exclusions>
	            <!-- EssentialsX's legacy-NMS "Provider" submodules transitively
	                 pull in org.bukkit:bukkit:1.12.2-R0.1-SNAPSHOT, an ancient
	                 pre-1.20 Bukkit API jar. Since it's a different Maven
	                 coordinate than our own paper-api, Maven's version
	                 mediation doesn't apply between them: both land on the
	                 compile classpath, and whichever's org.bukkit.block.Sign
	                 class the compiler resolves first wins. The old jar
	                 lacks the newer Sign#getSide(Side) API this codebase
	                 uses, so it must be excluded rather than relied on to
	                 lose that race. -->
	            <exclusion>
	                <groupId>org.bukkit</groupId>
	                <artifactId>bukkit</artifactId>
	            </exclusion>
	        </exclusions>
	    </dependency>
	    <dependency>
		    <groupId>com.github.GriefPrevention</groupId>
		    <artifactId>GriefPrevention</artifactId>
		    <version>17.0.0</version>
		    <scope>provided</scope>
		</dependency>
		<dependency>
		    <groupId>com.griefdefender</groupId>
		    <artifactId>api</artifactId>
		    <version>2.1.1-SNAPSHOT</version>
		    <scope>provided</scope>
		    <exclusions>
		        <!-- The published jar already shades and relocates all of these
		             under com.griefdefender.lib.*; the unshaded transitives are
		             unneeded and some (e.g. net.kyori:event-api:5.0.0-SNAPSHOT)
		             are no longer resolvable from any configured repository. -->
		        <exclusion>
		            <groupId>*</groupId>
		            <artifactId>*</artifactId>
		        </exclusion>
		    </exclusions>
		</dependency>
	    <dependency>
		    <groupId>com.github.EtienneDx</groupId>
		    <artifactId>AnnotationConfig</artifactId>
		    <version>e9eab24</version>
		</dependency>
		<dependency>
		    <groupId>me.clip</groupId>
		    <artifactId>placeholderapi</artifactId>
		    <version>2.12.3</version>
		    <scope>provided</scope>
		</dependency>
        <dependency>
  			<groupId>com.sk89q.worldguard</groupId>
  			<artifactId>worldguard-bukkit</artifactId>
  			<version>7.0.18</version>
  			<scope>provided</scope>
  			<exclusions>
  			    <!-- Transitively pulls org.spigotmc:spigot-api:1.16.2-R0.1-SNAPSHOT
  			         (a pre-1.20 spigot-api, via worldguard-core), which can
  			         shadow our own paper-api's org.bukkit.block.Sign class on
  			         the compile classpath; see the EssentialsX exclusion
  			         above for the full explanation. -->
  			    <exclusion>
  			        <groupId>org.spigotmc</groupId>
  			        <artifactId>spigot-api</artifactId>
  			    </exclusion>
  			</exclusions>
		</dependency>
		<dependency>
  			<groupId>com.sk89q.worldedit</groupId>
  			<artifactId>worldedit-bukkit</artifactId>
  			<version>7.4.5</version>
  			<scope>provided</scope>
  			<exclusions>
  			    <!-- Same rationale as worldguard-bukkit above: pulls its own
  			         old transitive org.spigotmc:spigot-api. -->
  			    <exclusion>
  			        <groupId>org.spigotmc</groupId>
  			        <artifactId>spigot-api</artifactId>
  			    </exclusion>
  			</exclusions>
		</dependency>
		<!-- No explicit net.kyori:adventure-api / adventure-text-serializer-plain
		     dependency here (deliberately, as of this multi-target update):
		     this project's own source never imports net.kyori.adventure.*
		     directly (GriefDefender's public API methods use it in their
		     signatures, and paper-api's own org.bukkit.entity.Player
		     implements Adventure's Audience and similar interfaces, so
		     something on the classpath must provide it). A pinned version
		     here previously shadowed paper-api's own transitively-supplied
		     Adventure version via Maven's "nearest wins" mediation, which
		     broke compilation against the 26.x target specifically (paper-
		     api's Player interface referenced newer Adventure classes,
		     e.g. net.kyori.adventure.text.object.PlayerHeadObjectContents,
		     that the old pinned 4.18.0 jar didn't have (a "cannot access
		     ... class file ... not found"). paper-api (and spigot-api, on
		     targets where it's present) already exposes adventure-api and
		     adventure-text-serializer-plain as transitive API dependencies
		     at a version matched to that specific target, so leaving them
		     unpinned lets each target resolve its own correct version. -->
		<dependency>
      		<groupId>com.palmergames.bukkit.towny</groupId>
      		<artifactId>towny</artifactId>
      		<version>0.103.2.0</version>
     	 	<scope>provided</scope>
    	</dependency>
    	<dependency>
		  <groupId>org.bstats</groupId>
		  <artifactId>bstats-bukkit</artifactId>
		  <version>3.0.2</version>
		  <scope>compile</scope>
		</dependency>
	</dependencies>
	<profiles>
		<!-- paper-api is declared in both of these mutually-exclusive
		     profiles (rather than unconditionally in <dependencies> above)
		     so that, on the with-spigot-api leg, spigot-api can be ordered
		     immediately BEFORE it. Both jars are alternate builds of the
		     same org.bukkit.* API surface (paper-api is a superset; see
		     Paper's own paper-api build.gradle.kts, which declares an
		     outgoing "org.spigotmc:spigot-api" capability conflict at the
		     same version specifically to stop Gradle consumers combining
		     both). Maven has no equivalent capability-conflict concept, so
		     when both jars are on the compile classpath simultaneously,
		     whichever's org.bukkit.block.Sign (etc.) class the compiler
		     resolves first wins; that ordering follows dependency
		     declaration order. spigot-api-then-paper-api is the order this
		     project has always shipped with; declaring them out of order
		     previously broke compilation with a spurious
		     "cannot find symbol: method getSide(Side)" on
		     RealEstateSign.java, since the wrong Sign class shadowed the
		     other. -->
		<profile>
			<id>with-spigot-api</id>
			<activation>
				<property>
					<name>skip.spigot.api</name>
					<value>false</value>
				</property>
			</activation>
			<dependencies>
				<dependency>
					<groupId>org.spigotmc</groupId>
					<artifactId>spigot-api</artifactId>
					<version>${spigot.api.version}</version>
					<scope>provided</scope>
				</dependency>
				<dependency>
					<groupId>io.papermc.paper</groupId>
					<artifactId>paper-api</artifactId>
					<version>${paper.api.version}</version>
					<scope>provided</scope>
				</dependency>
			</dependencies>
		</profile>
		<!-- No spigot-api is published for the 26.x scheme (see the note on
		     skip.spigot.api in <properties> above), so this target only
		     ever needs paper-api. -->
		<profile>
			<id>paper-only</id>
			<activation>
				<property>
					<name>skip.spigot.api</name>
					<value>true</value>
				</property>
			</activation>
			<dependencies>
				<dependency>
					<groupId>io.papermc.paper</groupId>
					<artifactId>paper-api</artifactId>
					<version>${paper.api.version}</version>
					<scope>provided</scope>
				</dependency>
			</dependencies>
		</profile>
	</profiles>
</project>
